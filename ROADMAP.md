# QTP 路线图

> 本路线图描述 QTP 的分阶段建设计划与取舍依据。
> 战略层面的"为什么这么定位"见 [docs/20260529-001-产品定位与路线规划.md](./docs/20260529-001-产品定位与路线规划.md)。

## 指导原则

1. **窄而深，不广而浅。** 范围收窄到一条能闭环的垂直链路，把它做到别人复制不了，而不是把九个模块都铺成半成品。吸引人的是"一个别人做不到的瞬间"，不是功能清单的长度。
2. **顺序即护城河顺序。** 先通链路 → 再做"可信" → 再做"独一无二" → 再做"哇"。每一阶段都比上一阶段更难、更不可替代。
3. **能演示 > 功能全。** 每个阶段收尾都问一句："它让北极星演示更可信 / 更惊艳了吗？"不能，就降优先级。
4. **专业度集中在阶段 2、3。** 这两块（随机性回归判定 + 多轮上下文追踪）做扎实，项目就有硬实力；做浅，就退回"套了 API 的后台"。资源往这里倾斜。

## 北极星演示

整个路线图服务于这一个可反复演示的瞬间，所有取舍围绕它：

> 导入一段多轮对话 → 改一版 Prompt / 模型 → 跑回归 → 平台指出
> **"第 3 轮模型仍用了旧值 80（用户第 2 轮已改成 20），判定为真实退化（非抖动），大概率上下文未持久化"**，并给出证据链与修复方向。

---

## 阶段规划

### 阶段 0 · 定位收敛与对外叙事定稿
**目标**：钉死边界，作为后续所有取舍的依据。不写功能代码。
- 一句话定位 + 竞品定位矩阵（QTP vs Ragas / DeepEval / promptfoo / Langfuse，每个对手一句站得住的差异化）。
- 把详细需求规格说明书砍成 v0.1 真实范围（明确"做 / 不做"清单），其余进 Roadmap。
- **产出**：定位文档、收敛后的 v0.1 范围、竞品矩阵、README、本路线图、LICENSE。

### 阶段 1 · 最窄闭环（让链路活起来）
**目标**：端到端打通，哪怕判定还很朴素。
- 接入：**仅 1 种 Adapter**（HTTP JSON 或 OpenAI Compatible），够调通真实接口。
- 用例：**仅多轮 Golden Case**，逐轮输入 + 基础断言（mustContain / mustNotContain）。
- 执行：跑单个版本，落库（请求 / 响应 / 耗时快照）。
- 对比：基线 vs 候选，识别 新增失败 / 修复失败 / 持续失败。
- **暂不做**：单轮 / RAG / 安全 / 格式用例、统计分析、通知、角色体系、Judge 插件化。

### 阶段 2 · 随机性下的回归判定（开始显专业）
**目标**：让"退化"这个判断**可信**。这是与所有"打一次分"工具的分水岭。
- 同一用例多次采样，区分**真退化** vs **正常抖动**（稳定性评分 / 阈值 / 置信度）。
- 引入 REVIEW 状态：拿不准的不武断判 FAIL。
- **演示价值**：能说出"这条失败我有 90% 把握是真退化"——这句话 Ragas 给不了。

### 阶段 3 · 多轮上下文追踪（差异化定型）
**目标**：把北极星那一屏真正做出来，这是最独一无二的部分。
- 从对话抽取上下文变量、跨轮追踪更新（旧值 → 新值）。
- 实现 `mustUpdateContext` / `mustNotUseStaleContext` / 指代消解 / 纠错类断言。
- 检测"模型某轮引用了陈旧值"。
- **演示价值**：项目第一次有了"非选你不可"的理由。

### 阶段 4 · 失败诊断与证据链（把"哇"补满）
**目标**：不只说"第 3 轮错了"，还说"为什么、怎么改"。
- 逐轮 diff + 证据链（哪轮、哪句、违反哪个断言）。
- 可能原因 Top-N + 修复建议（用 LLM，**可靠度优先于花哨**，宁可少说也别瞎说）。
- 人工复核闭环：复核结论可覆盖判定、误报回流到评估策略优化。
- **风险**：这块最吸引人也最易翻车，可靠度是第一优先级。

### 阶段 5 · 资产飞轮回填（越用越值钱）
**目标**：补上资产来源，闭合飞轮。
- 会话录制 → 结构化提取 → 一键转多轮 Golden Case。
- 失败 / 复核结果沉淀回回归集。

### 阶段 6 · 对外采纳基建（系统成型后做）
- 面向开发者的 README 强化 + Roadmap。
- docker 一键起 + 预置 demo 数据（开箱即跑出北极星那一屏）。
- 演示录屏 / GIF。
- CLI / CI 接入（让开发者塞进发布流水线）。

### 阶段 7+ · Roadmap（明确"暂不做"）
写进 README 传递"有规划在认真做"的信号，而非九个半成品：
- 单轮 / RAG / 安全 / 格式用例类型
- 统计分析、审计日志、通知
- 多 Adapter（Dify / SSE / Custom Webhook）
- Judge 评分器插件化、用例变体生成
- 团队 / 项目空间、更细的权限模型

---

## 范围速查：v0.1 做 / 不做

| 维度 | v0.1 做 | v0.1 不做（进 Roadmap） |
|---|---|---|
| 用例类型 | 多轮 Golden Case | 单轮 / RAG / 安全 / 格式 / 工具调用 |
| Adapter | 1 种 | Dify / SSE / OpenAI Compatible / Custom |
| 评估 | 规则断言 + 基础判定 | Judge 插件化、混合评估策略 |
| 资产来源 | 手工 / 导入 | 会话录制（阶段 5） |
| 平台能力 | 执行 + 版本对比 + 失败诊断 | 统计分析 / 通知 / 审计 / 角色体系 |

## 与现有工具的边界

QTP **不**与评估指标库竞争。Ragas / DeepEval / promptfoo 的指标可作为 QTP 内部的一种评分器接入。QTP 的价值落在它们不碰的四层：真实对话资产化、版本回归对比与发布门禁、可解释失败诊断、不写代码的工作流。详见 [README 对比表](./README.md#和-ragas--promptfoo--langfuse-有什么不同)。
